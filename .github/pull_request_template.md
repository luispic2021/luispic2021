## Summary

<!-- What changed, and why. -->

## Test plan

- [ ] `bundle exec jekyll build --trace` — no `YAML Exception`, `Liquid Warning`, or `Liquid Exception` in the output
- [ ] Spot-checked the affected page(s) in `_site/`
